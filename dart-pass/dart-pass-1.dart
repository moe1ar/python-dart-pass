main() {
  var hour = DateTime.now().hour;
  print(hour);
}

/*
We must specify the data type and the data type 'const' cannot be specified for a variable value such as time,
so we must set the data type of a variable such as 'var'.
*/
